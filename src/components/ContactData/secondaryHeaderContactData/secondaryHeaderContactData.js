import React from 'react';

const secondaryHeaderContactData = () => <h2 style={{ fontWeight: "800", fontSize: "14px", lineHeight: "19px" }}> O Vás </h2>

export default React.memo(secondaryHeaderContactData);
